{
  "name": "cfd-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CFD Desk web app: project setup, geometry, boundary conditions and SimScale-style meshing on OpenFOAM (WSL)",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --strictPort"
  },
  "dependencies": {
    "@kitware/vtk.js": "^32.12.1"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "playwright": "^1.49.0"
  }
}
